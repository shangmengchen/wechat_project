<view class="pair-page safe-bottom">
  <view class="status-row">
    <text>9:41</text>
    <text>4G WiFi 100%</text>
  </view>

  <view class="pair-head">
    <view class="pair-title">Pair Up <text class="heart">♥</text></view>
    <view class="pair-subtitle">Create your shared space with your partner</view>
  </view>

  <view class="seg-card">
    <view bindtap="generateCode" class="seg {{mode === 'generate' ? 'active' : ''}}">Create code</view>
    <view bindtap="switchMode" data-mode="input" class="seg {{mode === 'input' ? 'active' : ''}}">Enter code</view>
  </view>

  <view wx:if="{{mode === 'input'}}">
    <view class="code-card card">
      <view class="hint">Enter the 6-digit code from your partner</view>
      <view class="code-boxes">
        <view wx:for="{{[0,1,2,3,4,5]}}" wx:key="*this" class="code-box">{{code[index] || ''}}</view>
      </view>
    </view>

    <view class="keyboard">
      <view wx:for="{{['1','2','3','4','5','6','7','8','9']}}" wx:key="*this" bindtap="tapKey" data-key="{{item}}" class="key">{{item}}</view>
      <view></view>
      <view bindtap="tapKey" data-key="0" class="key">0</view>
      <view bindtap="deleteKey" class="key delete">Delete</view>
    </view>

    <button bindtap="confirmPair" class="confirm {{code.length === 6 ? 'ready' : ''}}">Confirm pair</button>
  </view>

  <view wx:else class="share-card card">
    <view class="share-top">
      <view class="share-label">Send this code to your partner</view>
      <view class="share-badge {{canRegenerate ? 'expired' : 'active'}}">{{shareStatusText}}</view>
    </view>
    <view class="share-code">{{generatedCode}}</view>
    <view class="share-time">{{shareTime}}</view>
    <view class="share-rule">{{shareRuleText}}</view>
    <view class="share-actions">
      <button open-type="share" class="primary-btn share-action">Share code</button>
      <button bindtap="generateCode" class="secondary-btn share-action {{canRegenerate ? 'ready' : 'disabled'}}" disabled="{{!canRegenerate}}">
        {{regenerateButtonText}}
      </button>
    </view>
  </view>

  <view bindtap="enterDemo" class="skip">Enter demo →</view>
</view>
