# Mini Program Upload

1. Copy `upload.config.example.json` to `upload.config.json`.
2. Put the WeChat upload private key at the configured `privateKeyPath`.
3. Run `npm install`.
4. Run `npm run upload`.

This project uses `appid` `wxa7a74d3bbccceafd` by default.
